<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="myMobileApp.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15" >
        <Label Text="Заголовок:" FontSize="Medium" TextColor="{StaticResource ColorText}"/>
        <Label Text="{Binding Text}" FontSize="Small" TextColor="{StaticResource ColorText}"/>
        <Label Text="Описание:" FontSize="Medium" TextColor="{StaticResource ColorText}"/>
        <Label Text="{Binding Description}" FontSize="Small" TextColor="{StaticResource ColorText}"/>
        <Label Text="Важность:" FontSize="Medium" TextColor="{StaticResource ColorText}"/>
        <Label Text="{Binding Importance}" FontSize="Small" TextColor="{StaticResource ColorText}"/>
        <Label Text="Дата:" FontSize="Medium" TextColor="{StaticResource ColorText}"/>
        <Label Text="{Binding Date}" FontSize="Small" TextColor="{StaticResource ColorText}"/>
        <Label Text="Категория:" FontSize="Medium" TextColor="{StaticResource ColorText}"/>
        <Label Text="{Binding Category}" FontSize="Small" TextColor="{StaticResource ColorText}"/>
    </StackLayout>
    
</ContentPage>